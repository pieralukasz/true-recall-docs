# True Recall Docs & Site Refresh — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconcile the truerecall.app docs/landing with the current plugin (AI Assistant + new views), add a component-screenshot pipeline, and surface lucaspiera.com in nav + footer.

**Architecture:** Astro 5 + Starlight docs site in the `true-recall-docs` repo. Docs are Markdown under `src/content/docs/` (autogenerated sidebar per directory). Marketing/auth pages are Astro under `src/pages/` using `src/layouts/BaseLayout.astro`. Plugin source in the sibling `true-recall` repo is READ-ONLY reference for facts.

**Tech Stack:** Astro, Starlight (`@astrojs/starlight`), `starlight-theme-obsidian`, Markdown/MDX, Vercel. Build/verify: `npm run build`. No unit tests exist; "verification" = build passes + no broken internal links + facts match plugin source.

**Verification model (docs adaptation of TDD):** For each content task the "test" is: (a) `npm run build` succeeds, (b) `grep` finds no broken links to renamed/removed paths, (c) every `Settings → …` breadcrumb and feature claim is checked against the named plugin source file before writing. Verify-then-write, not write-then-hope.

**Reference source root:** `/Users/lukaszpiera/Projects/true-recall/true-recall/packages/`

---

## File Structure

**Create (new docs pages):**
- `src/content/docs/plugins/ai-assistant.md` — AI Assistant hub
- `src/content/docs/views/ai-inbox.md` — AI Inbox view
- `src/content/docs/views/knowledge-chat.md` — RAG chat view
- `src/content/docs/views/fsrs-simulator.md` — FSRS simulator view
- `src/content/docs/configuration/ink-integration.md` — Ink handwriting integration

**Create (components):**
- `src/components/starlight/Footer.astro` — docs-pages footer override

**Modify (docs content):**
- `src/content/docs/creation/custom-note-types.md` — add Note Type Manager + Card Types Editor sections
- `src/content/docs/plugins/ai-flashcard-generation.md` — → thin stub
- `src/content/docs/plugins/card-polish.md` — → thin stub
- `src/content/docs/plugins/overview.md` — add ai-assistant, mark two deprecated
- `src/content/docs/plugins/generation-presets.md` — note chips in Ask AI
- `src/content/docs/configuration/editor-integration.md` — fix Link Status Indicators section
- `src/content/docs/configuration/ai-settings.md` — add assistant* settings + command/button
- `src/content/docs/configuration/knowledge-base.md` — retriever-in-Assistant note
- `src/content/docs/creation/creating-flashcards.md` — AI Assistant as creation path

**Modify (housekeeping / site):**
- `SITEMAP.md` — add new pages
- `CLAUDE.md` (docs repo) — drop `(P)` fiction, document `<!-- TODO PHOTO -->` + screenshot process
- `VISUAL-CONTENT-TODO.md` — reflect done/pending after Phase 2
- `astro.config.mjs` — register `Footer` override
- `src/components/starlight/Header.astro` + `MobileMenuFooter.astro` — lucaspiera.com nav link
- `src/layouts/BaseLayout.astro` — lucaspiera.com nav + footer link
- `src/content/docs/index.mdx` — landing refresh (AI Assistant feature card + CTA)

---

## Task 0: Feature branch

**Files:** none (git only).

- [ ] **Step 1: Branch off main in the docs repo**

```bash
cd /Users/lukaszpiera/Projects/true-recall/true-recall-docs
git checkout -b feat/docs-site-refresh
```

- [ ] **Step 2: Commit the spec + plan already written under `docs/superpowers/`**

```bash
git add docs/superpowers/
git commit -m "docs: add spec and plan for docs/site refresh"
```

---

# PHASE 1 — Documentation content

Style rules (from docs `CLAUDE.md`): frontmatter with required `title` + `description`; a `:::caution[My Notes]` dev block right after frontmatter (stripped in prod); bold product/UI names on first mention; `<!-- TODO PHOTO -->` where a screenshot goes; end each page with a `## What to Read Next` list of 3-5 links. Add backlinks from 2-3 related pages for every new page.

## Task 1: AI Assistant hub page

**Files:**
- Create: `src/content/docs/plugins/ai-assistant.md`
- Reference: `true-recall/packages/core/src/ai/assistant/assistant.types.ts`, `true-recall/packages/obsidian/src/services/assistant/assistant.service.ts`, `true-recall/packages/core/src/ai/workflows/ai-workflow.ts`, `true-recall/packages/plugins/src/ai-assistant/index.ts`

- [ ] **Step 1: Verify facts against source**

Read the reference files and confirm before writing: thread states (`active | inbox | archived`); proposal types (`create_card`, `update_card`, `update_draft`, `append_to_note`, `create_note`, `insert_diagram`, `attach_images`); workflow kinds (`agent`, `generate-cards`, `modify-card`) from `listAIWorkflows`; the `ask-ai` toolbar button + tier `byok`. Do NOT invent tool names — list only tools present in `assistant-tools.ts`.

- [ ] **Step 2: Write the page**

Frontmatter:

```markdown
---
title: AI Assistant
description: Draft, review, and apply AI-generated flashcards and notes with the True Recall AI Assistant.
sidebar:
  order: 1
---
```

Required sections (outline, prose written at execution):
1. Opening — one sentence: the AI Assistant is the unified draft-and-approve AI workflow (replaces the old AI Generation + Card Polish flows).
2. **Threads** — active / inbox / archived; a thread's result is a working set of draft proposals; conversation continues in-thread.
3. **Entry points** — Ask AI popover (select text in a review card), Ask AI modal / "AI Draft Studio", the editor panel in the Quick Note Editor, and the `PanelAiStrip` deep-link ("N AI drafts for this note"). `<!-- TODO PHOTO -->`
4. **The composer & workflow chips** — one prompt box; presets appear as chips (agent / generate-cards / modify-card). Cross-link `plugins/generation-presets.md`.
5. **Proposals** — the proposal types; edit fields inline; Apply / Reject / Apply all; conflict detection; Retry with feedback; Undo AI.
6. **Deferring to the Inbox** — closing an unfinished thread hands it to the AI Inbox ("Later"); settled threads archive. Link `views/ai-inbox.md`.
7. **Vault evidence** — the knowledge retriever (`search_knowledge`) surfaces vault evidence; link `configuration/knowledge-base.md` + `views/knowledge-chat.md`.
8. `## What to Read Next` — ai-inbox, generation-presets, ai-settings, creating-flashcards.

- [ ] **Step 3: Verify build**

Run: `cd /Users/lukaszpiera/Projects/true-recall/true-recall-docs && npm run build`
Expected: build succeeds, new route `/plugins/ai-assistant/` emitted.

- [ ] **Step 4: Commit**

```bash
git add src/content/docs/plugins/ai-assistant.md
git commit -m "docs: add AI Assistant hub page"
```

## Task 2: AI Inbox view page

**Files:**
- Create: `src/content/docs/views/ai-inbox.md`
- Reference: `true-recall/packages/obsidian/src/views/assistant/AssistantInboxApp.tsx`, `true-recall/packages/obsidian/src/plugin/PluginCommands.ts` (command `open-assistant-inbox`)

- [ ] **Step 1: Verify facts** — confirm command id `open-assistant-inbox` / label "Open AI assistant inbox", per-thread + inbox-wide "Approve all", status pills, orphan tasks.

- [ ] **Step 2: Write the page** with frontmatter:

```markdown
---
title: AI Inbox
description: Batch-review, approve, or defer AI Assistant drafts from the AI Inbox view.
sidebar:
  order: 8
---
```

Sections: opening; opening the view (command **Open AI assistant inbox**); layout (thread list + orphan tasks) `<!-- TODO PHOTO -->`; approving (per-thread + inbox-wide "Approve all"); deferring/"Later"; auto-archive; `## What to Read Next` (ai-assistant, flashcard-panel, dashboard).

- [ ] **Step 3: Verify build** — `npm run build`, route `/views/ai-inbox/`.
- [ ] **Step 4: Commit** — `git commit -m "docs: add AI Inbox view page"`.

## Task 3: Knowledge Chat view page

**Files:**
- Create: `src/content/docs/views/knowledge-chat.md`
- Reference: `true-recall/packages/obsidian/src/views/chat/KnowledgeChatApp.tsx`, `true-recall/packages/obsidian/src/features/rag/ui/`, command `open-knowledge-chat`

- [ ] **Step 1: Verify facts** — `@`-context chips, messages with sources, index status, config panel; command "Chat with knowledge base".
- [ ] **Step 2: Write the page**:

```markdown
---
title: Knowledge Chat
description: Chat with your vault notes and flashcards using True Recall's RAG-powered Knowledge Chat.
sidebar:
  order: 9
---
```

Sections: opening (distinguish from `configuration/knowledge-base.md`, which covers indexing/settings); opening the view (command **Chat with knowledge base**); layout — input, `@`-context chips, messages with sources, index status, config panel `<!-- TODO PHOTO -->`; how it relates to the AI Assistant's Vault evidence; `## What to Read Next` (knowledge-base, ai-assistant).
- [ ] **Step 3: Verify build** — route `/views/knowledge-chat/`.
- [ ] **Step 4: Commit** — `git commit -m "docs: add Knowledge Chat view page"`.

## Task 4: FSRS Simulator view page

**Files:**
- Create: `src/content/docs/views/fsrs-simulator.md`
- Reference: `true-recall/packages/obsidian/src/views/simulator/SimulatorApp.tsx`, `true-recall/packages/obsidian/src/features/metrics/ui/simulator/`, command "Open FSRS simulator"

- [ ] **Step 1: Verify facts** — parameter bar, sliders, chart, results table, multi-sequence comparison.
- [ ] **Step 2: Write the page**:

```markdown
---
title: FSRS Simulator
description: Experiment with FSRS parameters and scheduling outcomes using the True Recall simulator.
sidebar:
  order: 10
---
```

Sections: opening; opening the view (command **Open FSRS simulator**); what you can adjust; reading the chart/table `<!-- TODO PHOTO -->`; comparison sequences; `## What to Read Next` (fsrs-algorithm, presets, statistics).
- [ ] **Step 3: Verify build** — route `/views/fsrs-simulator/`.
- [ ] **Step 4: Commit** — `git commit -m "docs: add FSRS Simulator view page"`.

## Task 5: Ink integration page

**Files:**
- Create: `src/content/docs/configuration/ink-integration.md`
- Reference: `true-recall/packages/obsidian/src/editor/shared/ink-embeddable-editor.ts`, `true-recall/packages/obsidian/src/settings/tabs/IntegrationsTab.tsx` (`InkIntegrationSection`)

- [ ] **Step 1: Verify facts** — status states (`ready | incompatible | disabled | not-installed`); requires the third-party **Ink** plugin; enabled under **Settings → True Recall → Integrations → Ink Integration**; embeds render in review + editors.
- [ ] **Step 2: Write the page**:

```markdown
---
title: Ink Integration
description: Render handwritten and hand-drawn Ink embeds inside True Recall review and editors.
sidebar:
  order: 6
---
```

Sections: opening; prerequisites (install Ink plugin); enabling (`Settings → True Recall → Integrations → Ink Integration`); what renders where `<!-- TODO PHOTO -->`; status states + troubleshooting; `## What to Read Next` (editor-integration, image-occlusion, review-interface).
- [ ] **Step 3: Verify build** — route `/configuration/ink-integration/`.
- [ ] **Step 4: Commit** — `git commit -m "docs: add Ink integration page"`.

## Task 6: Extend custom-note-types with manager + card-types editor

**Files:**
- Modify: `src/content/docs/creation/custom-note-types.md`
- Reference: `true-recall/packages/obsidian/src/modals/core/note-type-manager/`, `true-recall/packages/obsidian/src/modals/core/card-types-editor/`, command "Manage note types"

- [ ] **Step 1: Read the current page** to match its heading style and avoid duplication.
- [ ] **Step 2: Verify facts** — Note Type Manager = CRUD over note types (command **Manage note types**); Card Types Editor = edit a note type's card templates/layouts.
- [ ] **Step 3: Add two sections** near the end (before "What to Read Next"): `## Managing Note Types` (the manager modal `<!-- TODO PHOTO -->`) and `## Editing Card Templates` (the Card Types Editor). Add cross-links to `views/flashcard-editor.md`.
- [ ] **Step 4: Verify build** — `npm run build`.
- [ ] **Step 5: Commit** — `git commit -m "docs: document Note Type Manager and Card Types Editor"`.

## Task 7: Stub the deprecated plugin pages

**Files:**
- Modify: `src/content/docs/plugins/ai-flashcard-generation.md`
- Modify: `src/content/docs/plugins/card-polish.md`

- [ ] **Step 1: Rewrite `ai-flashcard-generation.md`** to a thin stub. Keep frontmatter `title` + `description`; replace body with a `:::note[Now part of AI Assistant]` aside explaining AI card generation is now handled by the unified **AI Assistant** (`generate-cards` workflow), a one-line pointer, and a link to `plugins/ai-assistant.md`. Preserve any inbound anchor names the old page used if referenced elsewhere (grep first — Step 3).
- [ ] **Step 2: Rewrite `card-polish.md`** the same way (points to AI Assistant `modify-card` workflow).
- [ ] **Step 3: Grep for inbound links** to be sure the stubs still satisfy them:

Run: `grep -rn "ai-flashcard-generation\|card-polish" src/content/docs/`
Expected: every hit either points at the (now-stub) page — fine — or should be updated to `ai-assistant`. Update prose links that describe active functionality to point to AI Assistant.

- [ ] **Step 4: Verify build** — `npm run build`, both routes still emit.
- [ ] **Step 5: Commit** — `git commit -m "docs: stub deprecated AI Generation and Card Polish pages"`.

## Task 8: Plugins overview + generation-presets updates

**Files:**
- Modify: `src/content/docs/plugins/overview.md`
- Modify: `src/content/docs/plugins/generation-presets.md`
- Reference: `true-recall/packages/plugins/src/{ai-assistant,ai-generation,card-polish}/index.ts` (tiers + deprecated flags)

- [ ] **Step 1: Verify facts** — `ai-assistant` tier `byok`; `ai-generation` + `card-polish` carry `deprecated` + `replacementId: ai-assistant`.
- [ ] **Step 2: Edit `overview.md`** — add an **AI Assistant** row/entry (tier BYOK); mark AI Flashcard Generation + Card Polish as *Deprecated — superseded by AI Assistant* with links.
- [ ] **Step 3: Edit `generation-presets.md`** — add a sentence that generation presets now also appear as chips in the AI Assistant composer; link `plugins/ai-assistant.md`.
- [ ] **Step 4: Verify build** — `npm run build`.
- [ ] **Step 5: Commit** — `git commit -m "docs: update plugin overview and generation presets for AI Assistant"`.

## Task 9: Fix editor-integration (Link Status Indicators)

**Files:**
- Modify: `src/content/docs/configuration/editor-integration.md`
- Reference: `true-recall/packages/plugins/src/link-status-indicators/index.ts` (no `settingsPanel`), `true-recall/packages/obsidian/src/settings/tabs/GeneralTab.tsx` (Editor integration toggles)

- [ ] **Step 1: Verify facts** — the plugin has NO settings panel (enable/disable only in the Plugins accordion). Real toggles are in **Settings → True Recall → General → Editor integration**: `Show link status indicators`, `Show donuts in flashcard panel`, `Show donuts in review`, `Show status bar widget`. Display is context-driven: donut charts in the editor, text counts in reading view; hover shows the **Note Stats Tooltip**.
- [ ] **Step 2: Rewrite** the Link Status Indicators section — remove the "Display Modes: Text / Donut / Both" table (inaccurate), fix the breadcrumb to point at `General → Editor integration`, keep the Note Stats Tooltip description. `<!-- TODO PHOTO -->` for the tooltip.
- [ ] **Step 3: Verify build** — `npm run build`.
- [ ] **Step 4: Commit** — `git commit -m "docs: correct Link Status Indicators settings and display modes"`.

## Task 10: AI settings + knowledge-base + creating-flashcards updates

**Files:**
- Modify: `src/content/docs/configuration/ai-settings.md`
- Modify: `src/content/docs/configuration/knowledge-base.md`
- Modify: `src/content/docs/creation/creating-flashcards.md`
- Reference: `true-recall/packages/obsidian/src/settings/tabs/PluginsTab.tsx` (AIProviderSection), assistant settings keys in `true-recall/packages/core/src/ai/` / settings schema

- [ ] **Step 1: Verify facts** — confirm the exact `assistant*` setting keys exist before naming them (search settings schema for `assistantWebSearch`, `assistantMaxIterations`, `assistantMaxSources`, `assistantInstructions`). Only document keys that actually exist; if a name differs, use the real one. Confirm the `ask-ai` toolbar button + command `open-assistant-inbox`.
- [ ] **Step 2: Edit `ai-settings.md`** — add an "AI Assistant settings" subsection (only verified keys), mention the `ask-ai` toolbar button and **Open AI assistant inbox** command. Keep the (correct) `Settings → True Recall → Plugins` breadcrumb.
- [ ] **Step 3: Edit `knowledge-base.md`** — add a short note that the retriever also powers the AI Assistant's Vault evidence (`search_knowledge`); cross-link `plugins/ai-assistant.md` + `views/knowledge-chat.md`.
- [ ] **Step 4: Edit `creating-flashcards.md`** — add AI Assistant as an AI creation path with a link.
- [ ] **Step 5: Verify build** — `npm run build`.
- [ ] **Step 6: Commit** — `git commit -m "docs: document AI Assistant settings, retriever, and creation path"`.

## Task 11: Housekeeping — SITEMAP + docs CLAUDE.md

**Files:**
- Modify: `SITEMAP.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: Update `SITEMAP.md`** — add the 5 new pages under their sections with one-line descriptions; note the two stubbed pages.
- [ ] **Step 2: Update `CLAUDE.md`** — delete the "Sidebar Label Suffixes (Dev Only)" `(P)` section (it describes a convention that does not exist). Replace with: the real marker is `<!-- TODO PHOTO -->`; document the Phase 2 screenshot process and the `src/assets/screenshots/<area>/<name>.png` + `![alt](…)` embed convention.
- [ ] **Step 3: Verify** — `grep -rn "(P)" CLAUDE.md` returns nothing about sidebar suffixes; `npm run build` passes.
- [ ] **Step 4: Commit** — `git commit -m "docs: sync SITEMAP and CLAUDE conventions with reality"`.

---

# PHASE 2 — Component screenshot pipeline

## Task 12: Probe obsidian-cli screenshot capability

**Files:** none yet (investigation).

- [ ] **Step 1: Confirm Obsidian is running with the plugin** (the `obsidian-cli` skill requires an open vault). If not available, record that and skip to the manual-process fallback (Step 4).
- [ ] **Step 2: Invoke the `obsidian:obsidian-cli` skill** and attempt to (a) run a command that opens a True Recall view and (b) capture a screenshot of a single view (e.g. the Dashboard).
- [ ] **Step 3: Record the outcome honestly** in `VISUAL-CONTENT-TODO.md` under a new "## Capture method" heading: works / partially / not viable, with the exact reason.
- [ ] **Step 4: If not viable, write the manual process** instead (clean vault, dark theme, 1920×1080, per existing production notes) — do not fabricate screenshots.
- [ ] **Step 5: Commit** — `git commit -m "docs: record screenshot capture method"`.

## Task 13: Capture + embed priority screenshots

**Files:**
- Create: `src/assets/screenshots/<area>/*.png` (per capture)
- Modify: doc pages replacing `<!-- TODO PHOTO -->` with `![alt](../../../assets/screenshots/…png)`
- Modify: `VISUAL-CONTENT-TODO.md`

Priority order (from `VISUAL-CONTENT-TODO.md` + new pages): Dashboard, Flashcard Panel, Review loop / Session Complete, Selection Toolbar, AI Assistant composer, AI Inbox, Statistics, Knowledge Chat, FSRS Simulator.

- [ ] **Step 1: Capture** each priority view via the method chosen in Task 12.
- [ ] **Step 2: Save** to `src/assets/screenshots/<area>/<name>.png`.
- [ ] **Step 3: Replace** the matching `<!-- TODO PHOTO -->` with a Markdown image + descriptive alt text. Verify the relative path resolves (depth: `content/docs/<section>/page.md` → `../../../assets/...`).
- [ ] **Step 4: Update `VISUAL-CONTENT-TODO.md`** — check off captured items; explicitly list any that were skipped (do not leave them implied-complete).
- [ ] **Step 5: Verify build** — `npm run build` succeeds and images resolve (no missing-asset errors).
- [ ] **Step 6: Commit** — `git commit -m "docs: add component screenshots for priority views"`.

---

# PHASE 3 — lucaspiera.com links + landing refresh

## Task 14: Author links in nav

**Files:**
- Modify: `src/components/starlight/Header.astro`
- Modify: `src/components/starlight/MobileMenuFooter.astro`
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: Read all three files** and match existing link markup/classes.
- [ ] **Step 2: Docs header** — add an external link to `https://lucaspiera.com` in the `.right-items` block of `Header.astro` (label e.g. "Lucas Piera" or "Author"), and mirror it into `MobileMenuFooter.astro`. Open in a new tab (`target="_blank" rel="noopener"`).
- [ ] **Step 3: Marketing nav** — add the same link in the `.nav-links` of `BaseLayout.astro`.
- [ ] **Step 4: Verify build** — `npm run build`; link renders on a docs page and a marketing page.
- [ ] **Step 5: Commit** — `git commit -m "feat: add lucaspiera.com link to site navigation"`.

## Task 15: Author links in footers

**Files:**
- Create: `src/components/starlight/Footer.astro`
- Modify: `astro.config.mjs`
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: Marketing footer** — add a `https://lucaspiera.com` link beside Privacy / Terms in the `<footer class="page-footer">` of `BaseLayout.astro`.
- [ ] **Step 2: Create `src/components/starlight/Footer.astro`** — render the Starlight default footer content (import and re-render `@astrojs/starlight/components/Footer.astro` as `Default`) plus a small "Made by Lucas Piera · lucaspiera.com" line. If wrapping the default is awkward, render a minimal custom footer with prev/next preserved.
- [ ] **Step 3: Register** the override in `astro.config.mjs` under the Starlight `components:` map: `Footer: './src/components/starlight/Footer.astro'`.
- [ ] **Step 4: Verify build** — `npm run build`; footer link appears on both docs and marketing pages.
- [ ] **Step 5: Commit** — `git commit -m "feat: add lucaspiera.com link to site footers"`.

## Task 16: Landing refresh for AI Assistant

**Files:**
- Modify: `src/content/docs/index.mdx`
- Reference (consistency): `src/pages/pricing.astro`

- [ ] **Step 1: Read `index.mdx`** — locate the `<CardGrid>` with 6 feature cards (currently includes "AI Flashcard Generation").
- [ ] **Step 2: Replace** the "AI Flashcard Generation" card with an **AI Assistant** card: title "AI Assistant", copy describing draft-and-approve card/note generation + polish in one place; link the card to `/plugins/ai-assistant/`.
- [ ] **Step 3: Refresh the CTA** copy near `.homepage-cta` to mention the AI Assistant without changing pricing numbers. Check it does not contradict `pricing.astro` (which mentions "AI Study Coach & Leech Doctor"). If a plans/FAQ block clearly helps, optionally use the existing unused `.homepage-plans` / `.homepage-faq` styles — keep minimal.
- [ ] **Step 4: Verify build** — `npm run build`; landing renders, card links to the new page.
- [ ] **Step 5: Commit** — `git commit -m "feat: feature AI Assistant on the landing page"`.

---

## Task 17: Final verification

- [ ] **Step 1: Full build** — `cd /Users/lukaszpiera/Projects/true-recall/true-recall-docs && npm run build`. Expected: success, all new routes present.
- [ ] **Step 2: Broken-link sweep** — `grep -rn "](/" src/content/docs/ | grep -iE "ai-flashcard-generation|card-polish"` and confirm remaining references are intentional (stub pages) not dead functionality links.
- [ ] **Step 3: Confirm success criteria** from the spec §7 (every drift row addressed, SITEMAP + CLAUDE synced, screenshot outcome recorded, lucaspiera.com in both navs + both footers).
- [ ] **Step 4: Push + open PR** (only when the user asks) — `git push -u origin feat/docs-site-refresh` then open a PR against `main`.

---

## Self-Review (author checklist — completed)

- **Spec coverage:** §1a gaps → Tasks 1-6; §1b stale → Tasks 7-10; §1d convention fixes → Task 11; §4 screenshots → Tasks 12-13; §5 links+landing → Tasks 14-16. All spec rows mapped.
- **Placeholder scan:** no "TBD/handle edge cases"; every content task names exact files, frontmatter, sections, and a verify command. Prose is intentionally written at execution (documented adaptation), with source-verification gated first.
- **Type/name consistency:** command ids (`open-assistant-inbox`, `open-knowledge-chat`), workflow kinds (`agent`/`generate-cards`/`modify-card`), and proposal type names are used identically across Tasks 1, 2, 3, 8, 10. Ink status states consistent in Task 5. `Footer` override name consistent between Task 15 Step 2/3.
- **Fact risk flagged:** Tasks 1, 8, 10 require verifying names against source before writing (assistant setting keys especially — do not invent).
